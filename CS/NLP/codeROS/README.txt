This program uses ROS to run the multiple program concurently.
First, you need to install ROS based on your Ubuntu version: http://wiki.ros.org/

To run the program, open three terminal called A, B, and C.
In A, run "roscore"
In B, run "python GUI.py"
In C, run "python Main.py"
Then, start typing in the terminal B.
